// declare two variables that has module scope
// one with numerical data type
// another with string data type

var foo = 3;
var bar = "var";

// delcare two variables that has block scope
// one for array of string
// another without any value

let yum = ["block", "sock", "rock"];
let park;

// declare a variable whose value cannot be changed

const change = 1;

// print out the value of all declared variables to console

console.log(foo, bar, yum, park, change);
